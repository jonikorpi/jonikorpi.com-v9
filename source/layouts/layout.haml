!!! 5
- ie_html class: "no-js", lang: "en" do
  %head
    %meta{charset: "utf-8"}
    %title
      = data.page.title

    %meta{name: 'viewport', content: 'width=device-width, initial-scale=1.0'}

    = stylesheet_link_tag "main"
    =# javascript_include_tag "modernizr"

    :javascript
      /*(function() {
        var config = {
          kitId: '',
          scriptTimeout: 3000
        };
        var h=document.getElementsByTagName("html")[0];h.className+=" wf-loading";var t=setTimeout(function(){h.className=h.className.replace(/(\s|^)wf-loading(\s|$)/g," ");h.className+=" wf-inactive"},config.scriptTimeout);var tk=document.createElement("script"),d=false;tk.src='//use.typekit.net/'+config.kitId+'.js';tk.type="text/javascript";tk.async="true";tk.onload=tk.onreadystatechange=function(){var a=this.readyState;if(d||a&&a!="complete"&&a!="loaded")return;d=true;clearTimeout(t);try{Typekit.load(config)}catch(b){}};var s=document.getElementsByTagName("script")[0];s.parentNode.insertBefore(tk,s)
      })();*/

    = favicon_tag ''
    = favicon_tag '', rel: 'apple-touch-icon', type: 'image/png'
    %meta{name: "msapplication-TileImage", content: ""}
    %meta{name: "msapplication-TileColor", content: ""}
    %meta{property: "og:image",            content: ""}

  %body{class: "#{page_classes}"}
    = yield

    = javascript_include_tag "//ajax.googleapis.com/ajax/libs/jquery/1.9.0/jquery.min.js"
    :javascript
      /*if (typeof jQuery == 'undefined') {
        document.write(unescape("%3Cscript src='/javascripts/jquery.min.js'%3E%3C/script%3E"));
      }*/
    = javascript_include_tag "main"
